[metadata]
name        = Ticket Support
version     = 1.1.0
author      = Rafli (Fli)
description = Full-featured support ticket system with SMTP email notifications. Configure via Web Dashboard.

[commands]
triggers = .ticket, /ticket

[dependencies]
min_core_version = 1.0.0

[settings]
enabled = true